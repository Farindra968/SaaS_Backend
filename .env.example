PORT = 2000
DATABASE_NAME = "YourDatabaseName"
DATABASE_USER = "YourDatabaseUser"
DATABASE_PASSWORD = "YourDatabasePassword"
DATABASE_HOST = "YourDatabaseHost"
DATABASE_PORT = 5432
# Optional: Uncomment the following line if you want to use a different database type
# DATABASE_TYPE = "postgresql"  # or "mysql", "sqlite", etc.
# Optional: Uncomment the following line if you want to use a different environment
# ENVIRONMENT = "development"  # or "production", "testing", etc.
# Optional: Uncomment the following line if you want to set a secret key for your application
